{
  "name": "resticle-test-client",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "scripts": {
    "prebuild": "rm -rf build",
    "build": "tsc -p tsconfig.build.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Steven Sojka",
  "license": "MIT",
  "peerDependencies": {
    "resticle": "^1.0.0"
  },
  "devDependencies": {
    "reflect-metadata": "^0.1.9",
    "resticle": "^1.0.0",
    "typescript": "^2.1.4"
  }
}
